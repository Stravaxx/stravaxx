# Example environment variables for local development
# Create a local `.env` file (not committed) and fill the tokens there.

# Token for GitHub API (optional). Prefix required by Vite: VITE_
VITE_GITHUB_TOKEN=

# Token for GitLab API (optional)
VITE_GITLAB_TOKEN=

# Token for Gitea API (optional)
VITE_GITEA_TOKEN=
